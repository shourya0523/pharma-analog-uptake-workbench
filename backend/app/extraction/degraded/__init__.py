"""Degraded mode: the header grammar and regex prose reader, for runs with no model.

Never scored, never used to overrule or tie-break a description. Every
rule in this package reads a document's meaning from vocabulary and is,
by the project's own finding, shaped by the documents it was developed
against. Changes here move no gate.
"""
